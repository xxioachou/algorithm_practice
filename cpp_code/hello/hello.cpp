#include <bits/stdc++.h>
using namespace std;

void solve() {
    int x;
    cin >> x;
    cout << "Hello, VS Code!\n" << x << '\n';
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);
    int t = 1;
    // cin >> t;
    while (t--) solve();
    return 0;
}